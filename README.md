# Three Moons — Shrikant Garg

Portfolio: https://siliconalchemist.github.io/

This repository contains the generated static website for GitHub Pages. The portfolio source is maintained separately in a private repository.

GitHub Pages serves the `main` branch from the repository root. `.nojekyll` preserves the generated `_next` assets.

This is a published snapshot; future source changes need a new static export copied here to update this mirror.
